# Dockerfile for Node.js Express & SQLite backend
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY server/ ./server/
EXPOSE 5000
ENV NODE_ENV=production
CMD ["node", "server/server.js"]
